# --- core (offline pipeline + evaluation) ---
pyyaml>=6.0
scikit-learn>=1.3
numpy>=1.24
matplotlib>=3.7

# --- the governance service (gateway) ---
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6

# --- demo UI ---
streamlit>=1.30

# --- real LLM in the loop (free tier); enable with GROQ_API_KEY ---
groq>=0.9.0          # optional: AI-as-judge backend (scripts/test_judge.py)

# --- optional detector upgrades (not installed by default — see README) ---
#
# The pipeline is detector-agnostic: every detector returns a plain risk score
# in [0,1], so any of these can be swapped in without touching decision_theory,
# policy, session, or anomaly. Kept optional because they add real weight
# (torch alone is 500MB-1GB) and a network dependency on first run, which would
# break the "clone and reproduce with zero setup" guarantee the core pipeline
# gives everything reported in this README.
#
# transformers>=4.40              # NLI entailment grounding      (CONTROLPLANE_GROUNDING=nli)
# torch>=2.2                      #   backend for transformers + detoxify
# sentence-transformers>=2.2      # embedding grounding, AUROC 0.740 vs 0.708 for
#                                  # TF-IDF (CONTROLPLANE_GROUNDING=embedding)
# presidio-analyzer>=2.2          # production PII detection      (CONTROLPLANE_PII=presidio)
# presidio-anonymizer>=2.2
# detoxify>=0.5                   # learned toxicity/bias, replaces the lexicon
#                                  # in ResponsibilityDetector (CONTROLPLANE_TOXICITY=detoxify)
